extends layout

block content
  h1= title
  p Submit new message
    form
        div.input
            span.label Name
            input(type="text", name="name")
        .input
            span.label Message
            input(type="text", name="message")
        div.actions
            input(type="submit", value="send")
